<?php
//session_start();
// Check if employee is logged in
//if (!isset($_SESSION['EmployeeID']) || empty($_SESSION['EmployeeID'])) {
    // Use absolute path from server root
   // $absolute_path = '/stockport/employee-login.php?error=Please login first';
    
    // Redirect to login page if not logged in
 // header('location: ' . $absolute_path);
//    exit;
//}
?>